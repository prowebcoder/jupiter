{%- assign sid = '#shopify-section-' | append: section.id -%}

{%- style -%}
  {{ sid }} .pc-video-carousel {
    position: relative;
    width: 100%;
    overflow: hidden;
    padding: {{ section.settings.padding_top }}px {{ section.settings.padding_right }}px {{ section.settings.padding_bottom }}px {{ section.settings.padding_left }}px;
    margin: {{ section.settings.margin_top }}px {{ section.settings.margin_right }}px {{ section.settings.margin_bottom }}px {{ section.settings.margin_left }}px;
  }

  {{ sid }} .pc-video-carousel__header {
    text-align: {{ section.settings.heading_alignment }};
    margin-bottom: {{ section.settings.heading_spacing }}px;
  }

  {{ sid }} .pc-video-carousel__title {
    font-size: {{ section.settings.heading_font_size }}px;
    margin: 0 0 10px;
    color: {{ section.settings.heading_color }};
  }

  {{ sid }} .pc-video-carousel__subheading {
    font-size: {{ section.settings.subheading_font_size }}px;
    margin: 0;
    color: {{ section.settings.subheading_color }};
  }

  {{ sid }} .pc-video-carousel__container {
    display: flex;
    transition: transform 0.5s ease;
    width: 100%;
  }

  {{ sid }} .pc-video-carousel__slide {
    flex: 0 0 calc(100% / {{ section.settings.items_per_row }});
    padding: 0 {{ section.settings.gap | divided_by: 2 }}px;
    position: relative;
    border-radius: {{ section.settings.border_radius }}px;
    overflow: hidden;
  }

  {{ sid }} .pc-video-carousel__slide-inner {
    position: relative;
    border-radius: {{ section.settings.border_radius }}px;
    overflow: hidden;
  }

  {{ sid }} .pc-video-carousel__slide--portrait .pc-video-carousel__slide-inner {
    padding-bottom: 125%;
  }

  {{ sid }} .pc-video-carousel__slide--square .pc-video-carousel__slide-inner {
    padding-bottom: 100%;
  }

  {{ sid }} .pc-video-carousel__slide--landscape .pc-video-carousel__slide-inner {
    padding-bottom: 56.25%;
  }

  {{ sid }} .pc-video-carousel__slide--custom .pc-video-carousel__slide-inner {
    padding-bottom: {{ section.settings.video_aspect_ratio }};
  }

  {{ sid }} .pc-video-carousel__slide iframe,
  {{ sid }} .pc-video-carousel__slide video {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border: none;
    border-radius: {{ section.settings.border_radius }}px;
  }

  {{ sid }} .pc-video-carousel__controls {
    display: flex;
    justify-content: center;
    margin-top: 20px;
    gap: 10px;
  }

  {{ sid }} .pc-video-carousel__button {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: {{ section.settings.dot_color }};
    opacity: 0.5;
    cursor: pointer;
    border: none;
    padding: 0;
  }

  {{ sid }} .pc-video-carousel__button.active {
    opacity: 1;
  }

  {{ sid }} .pc-video-carousel__nav {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(0,0,0,0.5);
    color: white;
    border: none;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10;
  }

  {{ sid }} .pc-video-carousel__nav--prev {
    left: 20px;
  }

  {{ sid }} .pc-video-carousel__nav--next {
    right: 20px;
  }

  /* Single item view for mobile */
  @media (max-width: 768px) {
    {{ sid }} .pc-video-carousel {
      padding: {{ section.settings.mobile_padding_top }}px {{ section.settings.mobile_padding_right }}px {{ section.settings.mobile_padding_bottom }}px {{ section.settings.mobile_padding_left }}px;
      margin: {{ section.settings.mobile_margin_top }}px {{ section.settings.mobile_margin_right }}px {{ section.settings.mobile_margin_bottom }}px {{ section.settings.mobile_margin_left }}px;
    }

    {{ sid }} .pc-video-carousel__title {
      font-size: {{ section.settings.mobile_heading_font_size }}px;
    }

    {{ sid }} .pc-video-carousel__subheading {
      font-size: {{ section.settings.mobile_subheading_font_size }}px;
    }

    {{ sid }} .pc-video-carousel__slide {
      flex: 0 0 100%;
      padding: 0 {{ section.settings.mobile_gap | divided_by: 2 }}px;
    }

    {{ sid }} .pc-video-carousel__slide--portrait .pc-video-carousel__slide-inner {
      padding-bottom: 150%;
    }

    {{ sid }} .pc-video-carousel__slide--square .pc-video-carousel__slide-inner {
      padding-bottom: 100%;
    }

    {{ sid }} .pc-video-carousel__slide--landscape .pc-video-carousel__slide-inner {
      padding-bottom: 56.25%;
    }

    {{ sid }} .pc-video-carousel__slide--custom .pc-video-carousel__slide-inner {
      padding-bottom: {{ section.settings.mobile_video_aspect_ratio }};
    }

    {{ sid }} .pc-video-carousel__nav {
      width: 30px;
      height: 30px;
    }
  }

  /* Responsive items per row */
  @media (min-width: 769px) and (max-width: 1024px) {
    {{ sid }} .pc-video-carousel__slide {
      flex: 0 0 calc(100% / {{ section.settings.items_per_row_tablet }});
    }
  }
{%- endstyle -%}

<div class="pc-video-carousel color-{{ section.settings.color_scheme }}">
  {%- if section.settings.title != blank or section.settings.subheading != blank -%}
    <div class="pc-video-carousel__header">
      {%- if section.settings.title != blank -%}
        <h2 class="pc-video-carousel__title">{{ section.settings.title | escape }}</h2>
      {%- endif -%}
      {%- if section.settings.subheading != blank -%}
        <div class="pc-video-carousel__subheading">{{ section.settings.subheading }}</div>
      {%- endif -%}
    </div>
  {%- endif -%}

  <div class="pc-video-carousel__container" id="VideoCarousel-{{ section.id }}">
    {%- for block in section.blocks -%}
      <div
        class="pc-video-carousel__slide pc-video-carousel__slide--{{ block.settings.video_format }}"
        {{ block.shopify_attributes }}
      >
        <div class="pc-video-carousel__slide-inner">
          {%- if block.settings.video_url.type == 'youtube' -%}
            <iframe
              src="https://www.youtube.com/embed/{{ block.settings.video_url.id }}?autoplay={{ section.settings.autoplay }}&mute={{ section.settings.mute }}&loop={{ section.settings.loop }}&controls={{ section.settings.controls }}&playsinline=1"
              allow="autoplay; encrypted-media"
              allowfullscreen
            ></iframe>
          {%- elsif block.settings.video_url.type == 'vimeo' -%}
            <iframe
              src="https://player.vimeo.com/video/{{ block.settings.video_url.id }}?autoplay={{ section.settings.autoplay }}&muted={{ section.settings.mute }}&loop={{ section.settings.loop }}&controls={{ section.settings.controls }}&playsinline=1"
              allow="autoplay; encrypted-media"
              allowfullscreen
            ></iframe>
          {%- elsif block.settings.uploaded_video != blank -%}
            <video
              {% if section.settings.autoplay %}
                autoplay
              {% endif %}
              {% if section.settings.mute %}
                muted
              {% endif %}
              {% if section.settings.loop %}
                loop
              {% endif %}
              {% if section.settings.controls %}
                controls
              {% endif %}
              playsinline
            >
              <source src="{{ block.settings.uploaded_video }}" type="video/mp4">
              Your browser does not support the video tag.
            </video>
          {%- endif -%}
        </div>
      </div>
    {%- endfor -%}
  </div>

  {%- if section.settings.show_navigation -%}
    <button class="pc-video-carousel__nav pc-video-carousel__nav--prev" aria-label="Previous video">
      <svg
        xmlns="http://www.w3.org/2000/svg"
        aria-hidden="true"
        focusable="false"
        role="presentation"
        viewBox="0 0 24 24"
      >
        <path d="M15.41 16.58L10.83 12l4.58-4.59L14 6l-6 6 6 6 1.41-1.42z" fill="currentColor"/>
      </svg>
    </button>
    <button class="pc-video-carousel__nav pc-video-carousel__nav--next" aria-label="Next video">
      <svg
        xmlns="http://www.w3.org/2000/svg"
        aria-hidden="true"
        focusable="false"
        role="presentation"
        viewBox="0 0 24 24"
      >
        <path d="M8.59 16.58L13.17 12 8.59 7.41 10 6l6 6-6 6-1.41-1.42z" fill="currentColor"/>
      </svg>
    </button>
  {%- endif -%}

  {%- if section.settings.show_pagination -%}
    <div class="pc-video-carousel__controls">
      {%- for block in section.blocks -%}
        <button
          class="pc-video-carousel__button {% if forloop.first %}active{% endif %}"
          aria-label="Go to slide {{ forloop.index }}"
        ></button>
      {%- endfor -%}
    </div>
  {%- endif -%}
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const carousel = document.querySelector('#VideoCarousel-{{ section.id }}');
    const slides = document.querySelectorAll('#VideoCarousel-{{ section.id }} .pc-video-carousel__slide');
    const prevBtn = document.querySelector('{{ sid }} .pc-video-carousel__nav--prev');
    const nextBtn = document.querySelector('{{ sid }} .pc-video-carousel__nav--next');
    const dots = document.querySelectorAll('{{ sid }} .pc-video-carousel__button');
    let currentIndex = 0;
    const itemsPerRow = window.innerWidth > 1024 ? {{ section.settings.items_per_row }} : 
                        window.innerWidth > 768 ? {{ section.settings.items_per_row_tablet }} : 1;
    
    function updateCarousel() {
      const slideWidth = slides[0].offsetWidth;
      carousel.style.transform = `translateX(-${currentIndex * slideWidth}px)`;
      
      if (dots.length > 0) {
        dots.forEach((dot, index) => {
          dot.classList.toggle('active', index === currentIndex);
        });
      }
    }
    
    {% if section.settings.show_navigation %}
      prevBtn.addEventListener('click', () => {
        currentIndex = (currentIndex > 0) ? currentIndex - 1 : Math.ceil(slides.length / itemsPerRow) - 1;
        updateCarousel();
      });
      
      nextBtn.addEventListener('click', () => {
        const maxIndex = Math.ceil(slides.length / itemsPerRow) - 1;
        currentIndex = (currentIndex < maxIndex) ? currentIndex + 1 : 0;
        updateCarousel();
      });
    {% endif %}
    
    {% if section.settings.show_pagination %}
      dots.forEach((dot, index) => {
        dot.addEventListener('click', () => {
          currentIndex = index;
          updateCarousel();
        });
      });
    {% endif %}
    
    {% if section.settings.autoplay_carousel and section.settings.autoplay_delay > 0 %}
      let autoplayInterval = setInterval(() => {
        const maxIndex = Math.ceil(slides.length / itemsPerRow) - 1;
        currentIndex = (currentIndex < maxIndex) ? currentIndex + 1 : 0;
        updateCarousel();
      }, {{ section.settings.autoplay_delay | times: 1000 }});
      
      carousel.parentElement.addEventListener('mouseenter', () => {
        clearInterval(autoplayInterval);
      });
      
      carousel.parentElement.addEventListener('mouseleave', () => {
        autoplayInterval = setInterval(() => {
          const maxIndex = Math.ceil(slides.length / itemsPerRow) - 1;
          currentIndex = (currentIndex < maxIndex) ? currentIndex + 1 : 0;
          updateCarousel();
        }, {{ section.settings.autoplay_delay | times: 1000 }});
      });
    {% endif %}

    // Handle window resize
    window.addEventListener('resize', function() {
      updateCarousel();
    });
  });
</script>

{% schema %}
{
  "name": "Video Carousel",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Content Settings"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Video Gallery"
    },
    {
      "type": "richtext",
      "id": "subheading",
      "label": "Subheading",
      "default": "<p>Watch our latest videos</p>"
    },
    {
      "type": "select",
      "id": "heading_alignment",
      "label": "Heading alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "center"
    },
    {
      "type": "range",
      "id": "heading_spacing",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Heading bottom spacing",
      "default": 20
    },
    {
      "type": "header",
      "content": "Typography Settings"
    },
    {
      "type": "range",
      "id": "heading_font_size",
      "min": 12,
      "max": 50,
      "step": 1,
      "unit": "px",
      "label": "Heading font size (desktop)",
      "default": 24
    },
    {
      "type": "range",
      "id": "subheading_font_size",
      "min": 12,
      "max": 40,
      "step": 1,
      "unit": "px",
      "label": "Subheading font size (desktop)",
      "default": 16
    },
    {
      "type": "range",
      "id": "mobile_heading_font_size",
      "min": 12,
      "max": 40,
      "step": 1,
      "unit": "px",
      "label": "Heading font size (mobile)",
      "default": 20
    },
    {
      "type": "range",
      "id": "mobile_subheading_font_size",
      "min": 12,
      "max": 30,
      "step": 1,
      "unit": "px",
      "label": "Subheading font size (mobile)",
      "default": 14
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "subheading_color",
      "label": "Subheading color",
      "default": "#666666"
    },
    {
      "type": "header",
      "content": "Layout Settings"
    },
    {
      "type": "range",
      "id": "items_per_row",
      "min": 1,
      "max": 6,
      "step": 1,
      "label": "Items per row (desktop)",
      "default": 4
    },
    {
      "type": "range",
      "id": "items_per_row_tablet",
      "min": 1,
      "max": 4,
      "step": 1,
      "label": "Items per row (tablet)",
      "default": 2
    },
    {
      "type": "range",
      "id": "gap",
      "min": 0,
      "max": 50,
      "step": 2,
      "unit": "px",
      "label": "Space between items (desktop)",
      "default": 16
    },
    {
      "type": "range",
      "id": "mobile_gap",
      "min": 0,
      "max": 30,
      "step": 2,
      "unit": "px",
      "label": "Space between items (mobile)",
      "default": 8
    },
    {
      "type": "range",
      "id": "border_radius",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "label": "Border radius",
      "default": 8
    },
    {
      "type": "header",
      "content": "Video Settings"
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "Autoplay videos",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "mute",
      "label": "Mute videos",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "loop",
      "label": "Loop videos",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "controls",
      "label": "Show video controls",
      "default": true
    },
    {
      "type": "text",
      "id": "video_aspect_ratio",
      "label": "Custom aspect ratio (desktop)",
      "default": "56.25%",
      "info": "Enter as percentage (16:9 = 56.25%, 4:3 = 75%, 1:1 = 100%)"
    },
    {
      "type": "text",
      "id": "mobile_video_aspect_ratio",
      "label": "Custom aspect ratio (mobile)",
      "default": "75%",
      "info": "Enter as percentage (16:9 = 56.25%, 4:3 = 75%, 1:1 = 100%)"
    },
    {
      "type": "header",
      "content": "Carousel Controls"
    },
    {
      "type": "checkbox",
      "id": "show_navigation",
      "label": "Show navigation arrows",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_pagination",
      "label": "Show pagination dots",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "autoplay_carousel",
      "label": "Autoplay carousel",
      "default": false
    },
    {
      "type": "range",
      "id": "autoplay_delay",
      "min": 1,
      "max": 10,
      "step": 1,
      "unit": "s",
      "label": "Autoplay delay",
      "default": 5
    },
    {
      "type": "header",
      "content": "Style Settings"
    },
    {
      "type": "color",
      "id": "dot_color",
      "label": "Pagination dot color",
      "default": "#000000"
    },
    {
      "type": "select",
      "id": "color_scheme",
      "options": [
        {
          "value": "accent-1",
          "label": "Accent 1"
        },
        {
          "value": "accent-2",
          "label": "Accent 2"
        },
        {
          "value": "background-1",
          "label": "Background 1"
        },
        {
          "value": "background-2",
          "label": "Background 2"
        },
        {
          "value": "inverse",
          "label": "Inverse"
        }
      ],
      "default": "background-1",
      "label": "Color scheme"
    },
    {
      "type": "header",
      "content": "Desktop Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_left",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Left padding",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_right",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Right padding",
      "default": 0
    },
    {
      "type": "range",
      "id": "margin_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top margin",
      "default": 0
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom margin",
      "default": 0
    },
    {
      "type": "range",
      "id": "margin_left",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Left margin",
      "default": 0
    },
    {
      "type": "range",
      "id": "margin_right",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Right margin",
      "default": 0
    },
    {
      "type": "header",
      "content": "Mobile Spacing"
    },
    {
      "type": "range",
      "id": "mobile_padding_top",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Top padding",
      "default": 24
    },
    {
      "type": "range",
      "id": "mobile_padding_bottom",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Bottom padding",
      "default": 24
    },
    {
      "type": "range",
      "id": "mobile_padding_left",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Left padding",
      "default": 0
    },
    {
      "type": "range",
      "id": "mobile_padding_right",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Right padding",
      "default": 0
    },
    {
      "type": "range",
      "id": "mobile_margin_top",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Top margin",
      "default": 0
    },
    {
      "type": "range",
      "id": "mobile_margin_bottom",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Bottom margin",
      "default": 0
    },
    {
      "type": "range",
      "id": "mobile_margin_left",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Left margin",
      "default": 0
    },
    {
      "type": "range",
      "id": "mobile_margin_right",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Right margin",
      "default": 0
    }
  ],
  "blocks": [
    {
      "type": "video",
      "name": "Video",
      "settings": [
        {
          "type": "select",
          "id": "video_format",
          "label": "Video format",
          "options": [
            {
              "value": "portrait",
              "label": "Portrait"
            },
            {
              "value": "square",
              "label": "Square"
            },
            {
              "value": "landscape",
              "label": "Landscape"
            },
            {
              "value": "custom",
              "label": "Custom"
            }
          ],
          "default": "landscape"
        },
        {
          "type": "video_url",
          "id": "video_url",
          "accept": ["youtube", "vimeo"],
          "label": "Video URL",
          "default": "https://www.youtube.com/watch?v=_9VUPq3SxOc",
          "info": "Supports YouTube and Vimeo"
        },
        {
          "type": "text",
          "id": "uploaded_video",
          "label": "Shopify video URL",
          "info": "Upload a video in Settings > Files and paste the URL here"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Video Carousel",
      "blocks": [
        {
          "type": "video",
          "settings": {
            "video_url": "https://www.youtube.com/watch?v=_9VUPq3SxOc",
            "video_format": "landscape"
          }
        },
        {
          "type": "video",
          "settings": {
            "video_url": "https://vimeo.com/148751763",
            "video_format": "portrait"
          }
        },
        {
          "type": "video",
          "settings": {
            "video_url": "https://www.youtube.com/watch?v=9xwazD5SyVg",
            "video_format": "square"
          }
        },
        {
          "type": "video",
          "settings": {
            "video_url": "https://vimeo.com/148751763",
            "video_format": "custom"
          }
        }
      ]
    }
  ]
}
{% endschema %}
