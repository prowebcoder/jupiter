{%- assign sid = '#shopify-section-' | append: section.id -%}

{%- liquid
  assign video_position = section.settings.video_position
  assign background_color = section.settings.background_color
-%}

<style>
  {{ sid }} {
    background-color: {{ background_color }};
    padding: {{ section.settings.padding_top }}px 0 {{ section.settings.padding_bottom }}px;
  }

  {{ sid }} .pc-container {
    max-width: {{ section.settings.container_width }}px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    align-items: center;
    gap: {{ section.settings.grid_gap }}px;
  }

  {{ sid }} .pc-content-wrapper {
    order: {% if video_position == 'right' %}1{% else %}2{% endif %};
    height: 100%;
    align-content: center;
    background: {{ section.settings.content_background }};
    border-radius: {{ section.settings.content_radius }}px;
    padding: {{ section.settings.content_padding }}px;
  }

  {{ sid }} .pc-media-wrapper {
    order: {% if video_position == 'right' %}2{% else %}1{% endif %};
    position: relative;
  }

  {{ sid }} .pc-review-stars {
    font-size: {{ section.settings.review_size }}px;
    color: {{ section.settings.review_color }};
    margin-bottom: {{ section.settings.review_spacing }}px;
  }

  {{ sid }} .pc-heading {
    font-size: {{ section.settings.heading_size }}px;
    color: {{ section.settings.heading_color }};
    margin-bottom: {{ section.settings.heading_spacing }}px;
    line-height: {{ section.settings.heading_line_height }};
    font-weight: {{ section.settings.heading_weight }};
  }

  {{ sid }} .pc-subheading {
    font-size: {{ section.settings.subheading_size }}px;
    color: {{ section.settings.subheading_color }};
    margin-bottom: {{ section.settings.subheading_spacing }}px;
    max-width: {{ section.settings.subheading_width }}px;
    line-height: {{ section.settings.subheading_line_height }};
    font-weight: {{ section.settings.subheading_weight }};
  }

  {{ sid }} .pc-cta-button {
    background: {{ section.settings.button_color }};
    color: {{ section.settings.button_text_color }} !important;
    padding: {{ section.settings.button_padding }};
    border-radius: {{ section.settings.button_radius }}px;
    display: inline-flex;
    font-weight: {{ section.settings.button_weight }};
    text-transform: {{ section.settings.button_transform }};
    text-decoration: none;
    transition: transform 0.3s ease;
    font-size: {{ section.settings.button_size }}px;
  }

  {{ sid }} .pc-cta-button:hover {
    transform: scale(1.05);
  }

  {{ sid }} .pc-press-quotes {
    margin-top: {{ section.settings.press_spacing }}px;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax({{ section.settings.press_min_width }}px, 1fr));
    gap: {{ section.settings.press_gap }}px;
  }

  {{ sid }} .pc-press-item {
    font-size: {{ section.settings.press_size }}px;
    color: {{ section.settings.press_color }};
    position: relative;
    padding-left: 35px;
    line-height: 1.4;
  }

  {{ sid }} .pc-press-item:before {
    content: '';
    position: absolute;
    left: 0;
    top: 5px;
    width: 20px;
    height: 20px;
    background: {{ section.settings.press_icon_color }};
    mask: url('data:image/svg+xml;utf8,<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/></svg>');
  }

  {{ sid }} .pc-video-wrapper {
    aspect-ratio: {{ section.settings.video_aspect_ratio }};
    overflow: hidden;
    border-radius: {{ section.settings.media_radius }}px;
  }

  {{ sid }} .pc-video-wrapper video {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  @media (max-width: 768px) {
    {{ sid }} .pc-container {
      grid-template-columns: 1fr;
      gap: {{ section.settings.mobile_grid_gap }}px;
      padding: 0 {{ section.settings.mobile_container_padding }}px;
    }

    {{ sid }} .pc-content-wrapper {
      padding: {{ section.settings.mobile_content_padding }}px;
      border-radius: {{ section.settings.mobile_content_radius }}px;
    }

    {{ sid }} .pc-heading {
      font-size: {{ section.settings.mobile_heading_size }}px;
      margin-bottom: {{ section.settings.mobile_heading_spacing }}px;
    }

    {{ sid }} .pc-subheading {
      font-size: {{ section.settings.mobile_subheading_size }}px;
      margin-bottom: {{ section.settings.mobile_subheading_spacing }}px;
    }

    {{ sid }} .pc-review-stars {
      font-size: {{ section.settings.mobile_review_size }}px;
      margin-bottom: {{ section.settings.mobile_review_spacing }}px;
    }

    {{ sid }} .pc-cta-button {
      font-size: {{ section.settings.mobile_button_size }}px;
      width: 100%;
      text-align: center;
    }

    {{ sid }} .pc-press-quotes {
      grid-template-columns: 1fr;
      gap: {{ section.settings.mobile_press_gap }}px;
    }

    {{ sid }} .pc-press-item {
      font-size: {{ section.settings.mobile_press_size }}px;
    }
  }
</style>

<div class="pc-container">
  <div class="pc-media-wrapper">
    <div class="pc-video-wrapper">
      {%- if section.settings.video != blank -%}
        {{ section.settings.video | video_tag: autoplay: true, loop: true, muted: true, controls: false }}
      {%- else -%}
        {{ 'lifestyle-1' | placeholder_svg_tag: 'pc-placeholder-video' }}
      {%- endif -%}
    </div>
  </div>
  <div class="pc-content-wrapper">
    {%- if section.settings.reviews != blank -%}
      <div class="pc-review-stars">{{ section.settings.reviews }}</div>
    {%- endif -%}

    <h1 class="pc-heading">{{ section.settings.heading }}</h1>
    
    {%- if section.settings.subheading != blank -%}
      <div class="pc-subheading">{{ section.settings.subheading }}</div>
    {%- endif -%}

    {%- if section.settings.button_text != blank -%}
      <a href="{{ section.settings.button_link }}" class="pc-cta-button">
        {{- section.settings.button_text -}}
      </a>
    {%- endif -%}

    {% if section.blocks.size > 0 %}
      <div class="pc-press-quotes">
        {%- for block in section.blocks -%}
          <div class="pc-press-item" {{ block.shopify_attributes }}>
            {{ block.settings.quote }}
          </div>
        {%- endfor -%}
      </div>
    {% endif %}
  </div>
</div>

{% schema %}
{
  "name": "PWC - Hero with Video",
  "settings": [
    {
      "type": "header",
      "content": "Layout Settings"
    },
    {
      "type": "range",
      "id": "container_width",
      "min": 800,
      "max": 1800,
      "step": 20,
      "unit": "px",
      "label": "Container Width",
      "default": 1400
    },
    {
      "type": "range",
      "id": "grid_gap",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Column Gap",
      "default": 40
    },
    {
      "type": "range",
      "id": "video_aspect_ratio",
      "min": 0.5,
      "max": 2,
      "step": 0.1,
      "label": "Video Aspect Ratio",
      "default": 1
    },
    {
      "type": "header",
      "content": "Content Settings"
    },
    {
      "type": "text",
      "id": "reviews",
      "label": "Review Text",
      "default": "★★★★★ 4.9/5 from 10k+ Customers"
    },
    {
      "type": "richtext",
      "id": "heading",
      "label": "Heading",
      "default": "<p>Experience Deeper Sleep with Advanced Breathing Technology</p>"
    },
    {
      "type": "richtext",
      "id": "subheading",
      "label": "Subheading",
      "default": "<p>Clinically proven nasal strips that enhance oxygen intake by up to 38% during sleep. Non-invasive solution for better rest and improved performance.</p>"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "Shop Now - 30% Off"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "video",
      "id": "video",
      "label": "Video"
    },
    {
      "type": "select",
      "id": "video_position",
      "label": "Video Position",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "right", "label": "Right" }
      ],
      "default": "right"
    },
    {
      "type": "header",
      "content": "Style Customization"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#f7f9fc"
    },
    {
      "type": "color",
      "id": "content_background",
      "label": "Content Background",
      "default": "rgba(255, 255, 255, 0.95)"
    },
    {
      "type": "range",
      "id": "content_radius",
      "min": 0,
      "max": 50,
      "step": 2,
      "unit": "px",
      "label": "Content Radius",
      "default": 24
    },
    {
      "type": "range",
      "id": "media_radius",
      "min": 0,
      "max": 50,
      "step": 2,
      "unit": "px",
      "label": "Media Radius",
      "default": 24
    },
    {
      "type": "range",
      "id": "content_padding",
      "min": 20,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Content Padding",
      "default": 40
    },
    {
      "type": "header",
      "content": "Typography Settings"
    },
    {
      "type": "range",
      "id": "heading_size",
      "min": 24,
      "max": 72,
      "step": 2,
      "unit": "px",
      "label": "Heading Size",
      "default": 48
    },
    {
      "type": "range",
      "id": "heading_line_height",
      "min": 1,
      "max": 2,
      "step": 0.1,
      "label": "Heading Line Height",
      "default": 1.2
    },
    {
      "type": "range",
      "id": "heading_weight",
      "min": 300,
      "max": 900,
      "step": 100,
      "label": "Heading Font Weight",
      "default": 700
    },
    {
      "type": "header",
      "content": "Mobile Settings"
    },
    {
      "type": "range",
      "id": "mobile_container_padding",
      "min": 0,
      "max": 40,
      "step": 2,
      "unit": "px",
      "label": "Mobile Side Padding",
      "default": 16
    },
    {
      "type": "range",
      "id": "mobile_heading_size",
      "min": 18,
      "max": 42,
      "step": 2,
      "unit": "px",
      "label": "Mobile Heading Size",
      "default": 32
    }
  ],
  "blocks": [
    {
      "type": "press_quote",
      "name": "Press Quote",
      "settings": [
        {
          "type": "richtext",
          "id": "quote",
          "label": "Quote",
          "default": "<p>\"2024 Sleep Innovation Award Winner\" - HealthTech Journal</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "PWC - Hero with Video",
      "blocks": [
        {
          "type": "press_quote"
        },
        {
          "type": "press_quote"
        },
        {
          "type": "press_quote"
        }
      ]
    }
  ]
}
{% endschema %}